# Load lore-specific path configuration
eval "$(skogcli config export-env --namespace skogai,claude,openrouter,lore)"
# source config/paths.sh

# Activate Python virtual environment
source .venv/bin/activate

# Load environment from skogcli config
# This sets: SKOGAI, SKOGAI_LORE, SKOGAI_LOGS_DIR, CLAUDE_*, OPENAI_*
